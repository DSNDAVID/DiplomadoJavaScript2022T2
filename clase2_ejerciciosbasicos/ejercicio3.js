const prompt = require('prompt-sync')({sigint: true});

// esribe un programa que muestre el resultado de 3 + 5 

// prompt() sirve para pedir por consola que el usuario inserte valores.
// parseInt() sirve transformar tipos de datos string a numeros enteros. 

let num1 = 3; 
let num2 = 5;
let resultado = num1 + num2 ;
console.log(resultado)
